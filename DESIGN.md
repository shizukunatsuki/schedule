# 一日十二节 · 设计规则

整页只有一套规则，任何局部改动先对照这里，再动代码。

## 气质

空気系 / 日常系（芳文社一脉，带一点轻百合的粉 × 蓝）。白天的漫射光，大面积留白，颜色只作淡染，线条细且用同色系加深（色トレス），没有硬阴影、没有弹跳、没有高饱和。粉色是情绪中心，只留给「此刻」。

否决清单（不再尝试）：Material 语言、米色 + 赤陶、JIS 传统色和风、糖果粉彩 + 玻璃的三次元可爱、FGO / 阴阳师式高纯度 ACG。

## 色彩角色

| 角色 | 色相 | 填充 soft | 记号 mark ≥3:1 | 文字 text ≥4.5:1 |
|---|---|---|---|---|
| 上课 | 空色 sky | `#E2EFF9` | `#5590C2` | `#3D6E97` |
| 80 分钟休息 | 蜂蜜 honey | `#FFF3D6` | `#B3842A` | `#7A5A12` |
| 此刻 | 桜 sakura | `#FCE6ED` | `#CC6A8E` | `#A8416A` |
| 中性 | — | 底 `#F8F7F9` · 面 `#FFFFFF` · 凹 `#F3F2F5` | 发丝 `#8F8899` · 线 `#E9E4EC` | 墨 `#4A4652` · 次 `#6A6574` · 三 `#6E6878` |

三档规则：填充只承载面，记号承载条 / 点 / 描边，文字只用 text 档。每个组合在白、底、凹和各自 soft 上都算过 WCAG 2.2（1.4.3 / 1.4.11）。深色模式是同一张表的"夜"版，见 `index.html` 的 token 块。

一天的光（时间带专用）：行底按该行**偏移后的时刻**染 ≤ 6%——清晨偏桜、正午纯白、傍晚偏蜂蜜、夜里偏藤。染色只落在普通行上；此刻 / 休息 / 已过的行有自己的底，不叠加。

## 线与形

- 线：1px。中性用 `--line`；有色块的描边用该色相的 mark 档（色トレス）。
- 圆角：面板 14 · 控件 9 · 药丸 999。
- 阴影：只有一种，`0 6px 24px -18px rgba(120,90,110,.35)`。
- 光：页面左上一抹晨光粉、右上一抹天空蓝、右上两个虚焦光斑；固定不随滚动。

## 字

`ui-rounded`（Apple 上为 SF Rounded）→ 苹方 → Noto Sans SC。全站五档，对应 HIG：

| 档 | 用处 | 字号 / 字重 |
|---|---|---|
| Large Title | 页面标题 | 28–36 / 600 |
| Title | 此刻节名、偏移量 | 22–30 / 600 |
| Headline | 接下来、设置行标题、统计数字 | 17 / 600 |
| Body | 行名、说明 | 15 / 400（强调 600） |
| Footnote / Caption | 标签、读数、刻度 | 12 · 11 / 400–600 |

数字一律 `tabular-nums`；不用斜体、不用渐变字、不用全大写字距标签。

## 动

200–300ms `cubic-bezier(.25,.6,.3,1)` 缓出；时刻针针帽 2.6s 呼吸；没有弹簧、没有位移按压。`prefers-reduced-motion` 下全部关闭。

## UX 底线（Apple HIG · WCAG 2.2 · NN/g）

- 目标 ≥ 24px，触屏 44px；焦点环可见；键盘可完成全部操作。
- 标签句式、次级色；控件用平台语义（`range` / `radiogroup`）。
- 状态常驻可见（此刻卡、偏移与休息标签）；有复位；越界时夹回并说明。
- 双栏时左栏内容须在 820px 视口内放下；窄屏有紧凑条。
- 每次改动后：明暗 × 桌面 / 375 / 320 截图，DOM 对比度扫描 0 不达标。
